# Instructions for Claude workflow

The workflow is simply:
bashcd /mnt/user/appdata/goalscout
git add -A
git commit -m "description of changes"
git push
And to continue this project in a new Claude conversation, just say:

"I'm continuing the GoalScout project. The repo is at https://github.com/dazzopardi09/goalscout and the PROJECT-STATUS.md in the repo has the full context."

That file has everything — architecture, known issues, what's been built, what's next. No need to upload archives anymore.